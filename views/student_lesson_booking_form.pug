extend student_main

block content
  main
    h1= "Lesson booking"
    if name
      form(method='POST', action='') 
        label(for='name') Student name
        input(type='search', name='name' id='name', value=name, readonly=true)
        label(for='surname') Student surname
        input(type='search', name='surname', id='surname', value=surname, readonly=true)
        button(type='submit') Find
      each lesson in lessons 
        form(method='POST', action='')
          dl  
            dt #{lesson.style}
            dd #{lesson.teacher}
            dd #{lesson.day}
            dd #{lesson.time}
            dd #{lesson.classroom}
            dd #{lesson.number_spots} / #{lesson.booked_spots}
          input(type='hidden', name='name', value=name)
          input(type='hidden', name='surname', value=surname)
          input(type='hidden', name='lessonid', value=lesson._id)
          button(type='submit') Book
    else 
      form(method='POST', action='') 
        label(for='name') Student name
        input(type='search', name='name' id='name')
        label(for='surname') Student surname
        input(type='search', name='surname', id='surname')
        button(type='submit') Find
      
